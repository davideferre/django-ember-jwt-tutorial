<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">{{t "navbar.left.toggle_navigation"}}</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}{{t "navbar.left.app_name"}}{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
            {{#if session.isAuthenticated}}
                <li><a href="" {{ action 'invalidateSession' }}><i class="fa fa-sign-out fa-fw"></i>&nbsp;{{t "navbar.right.logout"}}</a></li>
            {{else}}
                <li>{{#link-to 'registration'}}<i class="fa fa-plus fa-fw"></i>&nbsp;{{t "navbar.right.register"}}{{/link-to}}</li>
                <li>{{#link-to 'login'}}<i class="fa fa-sign-in fa-fw"></i>&nbsp;{{t "navbar.right.login"}}{{/link-to}}</li>
            {{/if}}
        </ul>
    </div>
  </div>
</nav>
<div class="body">
    {{outlet}}
    {{ember-notify messageStyle='bootstrap' classPrefix="custom-notify"}}
</div>
<div class="footer">
    <div class="container-fluid">
        <div class="text-center">{{t 'footer.app_name'}}&nbsp;-&nbsp;<a href="{{t 'footer.credits_link'}}">{{t 'footer.credits_description'}}</a>&nbsp;&copy;&nbsp;2017</div>
    </div>
</div>
